import { FiterEtudiantsPipe } from './fiter-etudiants.pipe';

describe('FiterEtudiantsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterEtudiantsPipe();
    expect(pipe).toBeTruthy();
  });
});
